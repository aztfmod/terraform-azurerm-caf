resource "azurerm_synapse_workspace_extended_auditing_policy" "sywseap" {
  synapse_workspace_id                    = can(var.settings.synapse_workspace.id) ? var.settings.synapse_workspace.id : var.remote_objects.synapse_workspace[try(var.settings.synapse_workspace.lz_key, var.client_config.landingzone_key)][var.settings.synapse_workspace.key].id
  retention_in_days                       = try(var.settings.retention_in_days, null)
  storage_endpoint                        = can(var.settings.storage_accounts.key) == false ? try(var.settings.storage_accounts.endpoint, null) : var.remote_objects.storage_accounts[try(var.settings.storage_accounts.lz_key, var.client_config.landingzone_key)][var.settings.storage_accounts.key].primary_blob_endpoint
  storage_account_access_key              = can(var.settings.storage_accounts.key) == false ? try(var.settings.storage_accounts.access_key, null) : var.settings.storage_account_access_key_is_secondary ? var.remote_objects.storage_accounts[try(var.settings.storage_accounts.lz_key, var.client_config.landingzone_key)][var.settings.storage_accounts.key].primary_access_key : var.remote_objects.storage_accounts[try(var.settings.storage_accounts.lz_key, var.client_config.landingzone_key)][var.settings.storage_accounts.key].secondary_access_key
  storage_account_access_key_is_secondary = try(var.settings.storage_account_access_key_is_secondary, null)
  log_monitoring_enabled                  = try(var.settings.log_monitoring_enabled, null)
}