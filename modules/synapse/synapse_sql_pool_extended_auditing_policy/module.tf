resource "azurerm_synapse_sql_pool_extended_auditing_policy" "syspeap" {
  sql_pool_id                             = can(var.settings.sql_pool.id) ? var.settings.sql_pool.id : var.remote_objects.sql_pool[try(var.settings.sql_pool.lz_key, var.client_config.landingzone_key)][var.settings.sql_pool.key].id
  retention_in_days                       = try(var.settings.retention_in_days, null)
  storage_endpoint                        = can(var.settings.storage_accounts.key) == false ? try(var.settings.storage_accounts.endpoint, null) : var.remote_objects.storage_accounts[try(var.settings.storage_accounts.lz_key, var.client_config.landingzone_key)][var.settings.storage_accounts.key].primary_blob_endpoint
  storage_account_access_key              = can(var.settings.storage_accounts.key) == false ? try(var.settings.storage_accounts.access_key, null) : var.settings.storage_account_access_key_is_secondary == true ? var.remote_objects.storage_accounts[try(var.settings.storage_accounts.lz_key, var.client_config.landingzone_key)][var.settings.storage_accounts.key].primary_access_key : var.remote_objects.storage_accounts[try(var.settings.storage_accounts.lz_key, var.client_config.landingzone_key)][var.settings.storage_accounts.key].secondary_access_key
  storage_account_access_key_is_secondary = try(var.settings.storage_account_access_key_is_secondary, null)
  log_monitoring_enabled                  = try(var.settings.log_monitoring_enabled, null)
} 